# Data generation
datasets>=2.14.0
openai>=1.0.0
tqdm>=4.65.0

# Training (install unsloth separately: pip install unsloth)
trl>=0.12.0
transformers>=4.45.0
torch>=2.1.0
peft>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.44.0

# Export & upload
huggingface_hub>=0.25.0
llama-cpp-python>=0.3.0

# Inference / serving
vllm>=0.6.0

# Utilities
pandas>=2.0.0
numpy>=1.24.0
